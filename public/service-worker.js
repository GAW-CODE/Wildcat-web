//register new service worker


//install - cache resources in app shell (first load)

//activate - removes old resources from cache

//fetch - returns content from cache (subsequent loads), fall back to network if unavailable
